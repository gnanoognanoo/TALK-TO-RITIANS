# React Context Providers

Application-level state providers:
- `AuthContext.tsx`: Provides authenticated user state, loading status, and logout actions
- `ChatContext.tsx`: Manages active chat room socket/channel connection and messaging state
