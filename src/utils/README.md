# Utilities & Helpers

General-purpose pure utility functions:
- `qrParser.ts`: Extensible QR parser registry for college ID card payloads
- `hash.ts`: Cryptographic utilities (e.g. SHA-256 for deterministic ID hashing)
- `cn.ts`: Class name concatenation helper for Tailwind CSS
- `formatters.ts`: Date, time, and string formatters
